name: CI

on:
  push:
  pull_request:

jobs:
  testar:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o código
        uses: actions/checkout@v4

      - name: Executar teste inicial
        run: echo "Meu primeiro pipeline funcionou!"